<div class="container">
	<h1>TOPICS:</h1>
	<ul>
		<% @topics.each do |topic| %>
			<li><%= link_to "#{ topic.title }", topic_posts_path(topic) %> | <%= topic.description %></li>
		<% end %>
	</ul>
</div>